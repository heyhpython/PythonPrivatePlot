# coding=utf-8
def findRadius(houses, heaters):
    max_radius = (houses[-1] - houses[0]) // 2
    r = None
    for house in houses:
        if r is None:
            r =None


from prettyprinter import cpprint
